# Estado actual

Repo `{{project_name}}` inicializado desde la template `{{template_id}}`.

## Feature activa

Ninguna. F000 - Bootstrap inicial del repo queda preparada como registro de
creacion.

## Resultado del bootstrap

- Estructura base generada.
- `feature_list.json` inicializado.
- `progress/` inicializado.
- Pendiente ejecutar o registrar el resultado final de `./init.sh` si el agente
  de bootstrap todavia no lo ha hecho.

## Siguiente paso recomendado

Usar la skill `plan_product_features` para crear la epica inicial y dividir el
producto en features granulares.
